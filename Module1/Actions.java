package guis;

// Public class that holds an enum of all the choices a user can choose
public class Actions {
    public enum UserActions {
        OPENFOLDER, PLAYMUSIC, STOPMUSIC, MOVELABEL, STOPLABEL, STARTCLOCK, STOPCLOCK
    }
}
